# Tiness V0.1 测试报告

测试日期：2026-09-11。环境：macOS arm64，Bun 1.4.2。使用当前用户已配置的 Responses 连接；未修改全局配置，未在报告中保存模型凭据。

## 结果

| 验证 | 结果 | 范围 |
|---|---|---|
| `bun test` | 40 通过、0 失败、3 跳过 | 跳过的是需要显式启用的真实 API 测试；这三项已另行执行 |
| 真实模型基础闭环 | 通过，约 5.2 秒 | 读取 input.txt、创建 output.txt、再次读取验证 |
| 真实模型复杂任务 | 通过，约 13.3 秒 | 创建 Plan、运行失败测试、修改 fixture 源码、测试通过、FIFO 执行下一条消息 |
| 真实模型上下文压缩 | 修复后通过，约 17.6 秒 | 压缩合成历史、保持验收信息、将新上下文用于真实 Responses 请求 |
| PTY 终端集成 | 通过，包含在本地测试中 | 普通输入不会批准工具；审批后执行 shell；Esc 取消并清理；排队消息继续执行 |
| 本机独立二进制 | 启动与正常退出通过 | 读取实际配置、创建临时 Task、终端初始化、写入 task_end；该检查未调用模型 |
| 静态类型检查 | 通过 | 应用、脚本与测试源码 |
| 二进制构建 | 通过 | 本机及 darwin/linux × arm64/x64 |

真实模型场景分别执行，未为了得到一个合并数字而重复调用已通过的场景。三项在默认本地测试中显示 skip 是预期行为，不代表它们未验证。

## 发现并修复的问题

原压缩器直接把 `summaryMaxTokens` 作为 Responses 的 `max_output_tokens`。但推理模型的总输出预算还包含推理开销，导致一次真实压缩在输出 1024 tokens 后以 `incomplete / max_output_tokens` 结束，没有可见摘要。由于原输入已超本地预算，Session 正确停止，但无法完成压缩。

修复：

- 使用 `maxOutputTokens` 为摘要请求提供包含推理的总生成预算。
- `summaryMaxTokens` 仅用于限制被保留的可见摘要。
- 摘要提示明确可见长度目标，并要求合并重复观察。
- 保存 `context_compaction_failed` 及具体失败原因，避免只显示笼统超限信息。
- 增加“摘要长度限制不能耗尽推理预算”的回归测试。

修复后的真实压缩测试通过。失败时仍保持原始 JSONL 与上一份上下文投影，不使用空摘要覆盖历史。

## 终端界面优化验证

本次界面优化后重新运行本地测试与类型检查，40 项通过。真实 API 的三项结果来自此前 harness 验证，本次未重复调用。

- 100×32、80×24、40×16 的布局检查：中文宽度、三个授权选项完整可见、面板不覆盖输入框。
- 过小窗口不接受授权确认；已有草稿保留输入焦点，鼠标可直接选择授权。
- 工具输出默认折叠，F2 展开；滚轮翻阅历史，新消息不改变已有视口，End 返回最新。
- 实际 PTY 连接运行时：输入不误授权、明确授权后启动 shell、Esc 清理进程、FIFO 接续。
- 另用 pyte 解释实际 PTY 的控制序列，检查居中面板的可见内容，并验证长消息滚轮翻阅与 End 返回。修复了面板覆盖中文宽字符时残留背景文字的问题。

## 本地测试覆盖

- 配置字段与数值校验、全局/项目权限合并、凭据来源、配置只读与符号链接拒绝。
- FIFO 顺序、排队信息隔离、队列容量、Task 切换、失败与取消后的接续。
- 模型等待、审批等待及压缩中的取消；批量工具未执行项的结算。
- 最大 Round 数、Session 截止时间、有限重试与鉴权失败停止队列。
- 工具 schema、Plan 唯一性、原生文件边界、审批后的文件冲突。
- 长行分页、产物读取、输出限额、shell 取消与受管进程组清理。
- Responses 调用 ID 与结果配对、reasoning 项保留、不完整响应不执行工具。
- 上下文压缩、失败保护、当前请求和计划保留。
- Skill 发现与分段激活、JSONL 串行写入、跨输出分块的已知密钥过滤。
- 中文显示宽度、粘贴换行、审批焦点、Esc 与方向键区分、实际 PTY 联动。

## 复现

```bash
bun run typecheck
bun test
```

明确允许真实模型调用、且已经配置全局模型后：

```bash
TINESS_LIVE=1 bun test tests/live.test.ts
```

只重复压缩场景：

```bash
TINESS_LIVE=1 bun test tests/live.test.ts -t 'live context'
```

真实场景只在临时工作区使用 fixture 文件或合成历史。复杂修复审批限于指定源码和测试命令，不允许模型修改测试文件、项目仓库或全局配置。

## 验证边界

Linux、macOS x64 的二进制已构建，尚未在对应硬件或系统实机运行。PTY 验证覆盖输入与执行行为，不等同于所有终端尺寸、输入法和视觉呈现的人工验收。

无 OS 沙箱是当前设计边界；测试通过不构成 shell 隔离保证。未模拟断电、磁盘硬件故障或恶意进程持续竞争路径。API 成本、服务商限流和模型行为仍受实际端点影响。
